queue_rules:
  - name: vm3-parallel-bisect4-p4x8
    queue_conditions: []
    merge_conditions:
      - check-success = @github-actions/interaction-guard
    merge_method: merge
    batch_size: 4
    batch_max_wait_time: 30 seconds
    max_checks_retries: 0
merge_queue:
  mode: serial
  max_parallel_checks: 2
pull_request_rules: []
